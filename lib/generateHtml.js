const generateHtml = answer => {
    console.log(answer)
    // answer.forEach(member => member.getName())
    // return `Hello ${answer[0].manager}`
    return `Hello`
}

module.exports = generateHtml;